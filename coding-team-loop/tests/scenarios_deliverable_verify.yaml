# scenarios_deliverable_verify.yaml
# 对应 refs/deliverable-verify.md
# 覆盖：verifying+owner/codex 验收派发

- id: C02
  name: "Step 2-P3：有 verifying + owner/codex Issue，Claude 空闲 → 派发验收请求"
  issues:
    - number: 42
      label: verifying
      owner_label: owner/codex
  open_prs: []
  pane_claude: idle
  pane_codex: idle
  expect:
    step2_action: deliverable-verify
    dispatch_to: claude
    message_contains: ["验收", "42"]
